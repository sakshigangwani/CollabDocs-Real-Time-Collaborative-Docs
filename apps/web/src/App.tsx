import Landing from "./pages/Landing";

// The root component. For now it shows the public landing page.
// Once auth and routing are added, this is where we'll switch between
// the landing page, login, and the app itself.
export default function App() {
  return <Landing />;
}
